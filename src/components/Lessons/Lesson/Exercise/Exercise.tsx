import React from 'react'
import {IExerciseInfo} from '../../../../packets/api/TypeRequest'

import st from './Exercise.module.scss'

export const Exercise : React.FC<IExerciseInfo> = ( {name, number, progress, balls} ) => {


    return <div>

    </div>
}